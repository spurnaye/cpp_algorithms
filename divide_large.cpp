#include <vector>
#include <iostream>
#include <string>

std::vector<std::string> divide(std::string dividend, std::string divisor){
  std::string remainder = dividend;
  str::string quotient;
  while(remainder > divisor){
    std::string::iterator start = std::begin(divisor);
    std::string::iterator end = std::begin(divisor);
  }
  return {};
}
int main(){
  divide("219", "11");
  return 0;
}